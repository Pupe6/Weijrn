import * as React from "react";
import {
	Box,
	Center,
	Heading,
	VStack,
	FormControl,
	Input,
	Button,
	HStack,
	Text,
	Link,
} from "native-base";

export default function HomeScreen({ navigation }) {
	return (
		<Center flex={1}>
			<Text> kolqbn</Text>
		</Center>
	);
}
